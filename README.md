# How To Run
## In Repl.it
Set the `$replit` variable in `main.sh` (line 3) to `true` and run as usual.
## On The Pi
Set the `$replit` variable in `main.sh` (line 3) to `false`.
Enter the root directory of the project.
Run `./main.sh`.
Profit?
